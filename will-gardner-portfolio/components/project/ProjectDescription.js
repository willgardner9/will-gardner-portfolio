const ProjectDescription = ({projectDescription}) => {
  return (
    <h4 className="font-karla text-lg font-light leading-8 mt-4 dark:text-gray-400">
      {projectDescription}
    </h4>
  );
};

module.exports = ProjectDescription;
